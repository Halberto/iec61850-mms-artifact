# Results Layout

The `expected_outputs/` directory contains reference CSV and TXT outputs from the working repository. These files are included for sanity checking and artifact inspection.

The current bundle includes campaign summaries, packet summaries, temporal summaries, attack-sequence views, and per-IED breakdowns. Full paper-scale regeneration may require the separately published full dataset archive.
